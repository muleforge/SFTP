<?xml version="1.0" encoding="UTF-8"?>
<mule xmlns="http://www.mulesource.org/schema/mule/core/${m"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:$transportid="http://www.mulesource.org/schema/mule/$transportid/${m"
    xsi:schemaLocation="
       http://www.mulesource.org/schema/mule/core/${m http://www.mulesource.org/schema/mule/core/${m/mule.xsd
       http://www.mulesource.org/schema/mule/$transportid/${m http://www.mulesource.org/schema/mule/$transportid/${m/mule-$transportid.xsd">

    <$transportid:connector name="$transportidConnector">
        <!-- TODO Add connector specific properties here and then assert that 
             they have been set in org.mule.transport.$transportid.$transportidNamespaceHandlerTestCase -->
    </$transportid:connector>

</mule>