<?xml version="1.0" encoding="utf-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>org.mule.transports</groupId>
    <version>1.4.4-SNAPSHOT</version>
    <artifactId>mule-transport-sftp-dist</artifactId>
    <!-- This should be "pom" not "jar" but if it is set to "pom", the list of dependencies gets ignored
    by the assembly plugin. -->
    <packaging>jar</packaging>
    <name>SFTP Transport Distribution</name>
    <description>Includes SFTP Transport and Examples</description>
  <dependencies>
    <dependency>
      <groupId>org.mule.transports</groupId>
      <artifactId>mule-transport-sftp</artifactId>
      <version>${version}</version>
    </dependency>
    <dependency>
      <groupId>org.mule.tests</groupId>
      <artifactId>mule-tests-functional</artifactId>
      <version>${version}</version>
    </dependency>
    <dependency>
      <groupId>jsch</groupId>
      <artifactId>jsch</artifactId>
      <version>0.1.29</version>
    </dependency>
  </dependencies>
    <build>
        <defaultGoal>install</defaultGoal>
        <finalName>${artifactId}</finalName>
        <plugins>
            <plugin>
                <artifactId>maven-assembly-plugin</artifactId>
                <!--
                   Uncomment when it's published, contains wildcards functionality.
                   Don't want to go with a SNAPSHOT yet.
                 -->
                <!-- version>2.2</version -->
                <configuration>
                    <finalName>mule-transport-sftp-${version}</finalName>
                    <descriptors>
                        <descriptor>assembly.xml</descriptor>
                    </descriptors>
                    <appendAssemblyId>false</appendAssemblyId>
                </configuration>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>assembly</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
